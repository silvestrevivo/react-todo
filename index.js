import React, { Component } from 'react';
import ReactDOM from 'react-dom';
import './style.scss';

const App = () => {
    return <h1>To-do list with ReactJS and Parcel</h1>;
};

ReactDOM.render(<App />, document.getElementById('root'));
